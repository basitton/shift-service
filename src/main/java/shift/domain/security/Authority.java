package shift.domain.security;

/**
 * Provides a restricted set of constants that represent the application's allowed authorities
 */
public enum Authority {
    ROLE_MANAGER,
    ROLE_EMPLOYEE,
    ROLE_USER;
}
